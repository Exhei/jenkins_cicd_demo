{
  "name": "jenkins-cicd-demo",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/",
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}